 <?php
 use Zend\Mvc\Controller\Plugin\FlashMessenger;
 
 $title ="Versiones del Sistema" ;
 $this->headTitle($title);
 ?>

<?php echo $this->flashMessenger()->render(FlashMessenger::NAMESPACE_WARNING, array('alert','alert-warning')); ?>
<?php echo $this->flashMessenger()->render(FlashMessenger::NAMESPACE_INFO, array('alert', 'alert-info'));?>

<div class='panel panel-primary'>
    <div class='panel-heading'>
        <h1><?php echo $this->escapeHtml($title); ?></h1>
    </div>
</div>

 <p>
     <a class="btn btn-primary" href="<?php echo $this->url('version', array('action'=>'add'));?>">Agregar Nueva Version</a>
 </p>

 <table class="table table-hover">
     <thead>
         <tr class="active">
             <th>Version</th>
             <th>Fecha</th>
             <th>Accion</th>
         </tr>
     </thead>
     <tbody>
        <?php foreach ($versiones as $version): ?>
        <tr>
            <td><?php echo $this->escapeHtml($version->version);?></td>
            <td><?php echo $this->escapeHtml($version->fecha);?></td>
            <td>
               <a class="btn btn-lg btn-warning" target="_blank" href="<?php echo $this->url('version',
                   array('action'=>'view', 'id' => $version->id));?>">Descagar Fichero</a>
            </td>
        </tr>
        <?php endforeach; ?>
     </tbody>
 </table>